DEVICES{
    DTYPE dtype;
    SWITCH sw1(0), sw2(1), sw3(1);
    CLOCK clock(10);
    AND and(10), and(4);
}
CONNECT{
    sw1 => dtype.SET;
    sw2 => dtype.CLK;
    sw3 => dtype.SET;
    clock => dtype.DATA;
}
MONITOR{
    dtype.Q;
}
END
